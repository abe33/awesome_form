:ruby
  actions_html ||= {}

  fieldset_attrs = builder.attributes_for(:fieldset, actions_html, class: AwesomeForm.default_actions_class)

%fieldset{fieldset_attrs}
  - if defined? legend
    %legend
      / It's known fact that legend can't be styled well
      %span= legend

  = yield
